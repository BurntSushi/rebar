This small Rust library provides a `Benchmark` type that knows how to read and
write itself in the [KLV](../KLV.md) format. It's used by both rebar and some
benchmark harness programs written in Rust.
