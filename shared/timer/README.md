A small Rust library for running and measuring benchmark timings. Since there
are a number of benchmark runners written in Rust, it's useful to define these
routines once and reuse them in several places.
